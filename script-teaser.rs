// okay i have never used rust
// but i think i can do this

// this is what the script should do

// the inputs are "branch_name", "repo_name" and "github_username"

{/*
    1. attempts to add the repo to the BLSM db, if it already exists, it will return an error.
    2. it will then create the setup files for using BLSM
    
*/}